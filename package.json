{
  "name": "route-progress-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:routes && vite build",
    "preview": "vite preview",
    "build:routes": "node scripts/build-routes.mjs",
    "render:instagram": "node scripts/render-instagram.mjs"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10",
    "leaflet": "^1.9.4",
    "vite": "^8.0.10"
  }
}
